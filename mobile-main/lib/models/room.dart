class RoomModel {
  String title, asset, valor, description, detail;

  RoomModel(
      {required this.title,
      required this.asset,
      required this.valor,
      required this.description,
      required this.detail})
      : super();
}
